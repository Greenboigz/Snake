/**
 * Gets a random number between 0 and max
 * @param {number} max Maximum value that the number can be
 * @return {number}
 */
function getRandomInt(max) {
    return Math.floor(Math.random() * Math.floor(max));
}